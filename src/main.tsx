import React from "react";
import ReactDOM from "react-dom/client";
import Plums from "./pages/plums";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Plums />
  </React.StrictMode>
);
